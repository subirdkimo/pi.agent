# pi.agent

Repo for pi agent skills and automation workflow.

## What's Inside

- `SKILL.md` — `init-auto4pi.agent` skill covering:
  - `npm:pi-web-access` web access extension
  - `badlogic/pi-skills` (browser-tools, youtube-transcript, etc.)
  - Browser automation with Chrome (`--remote-debugging-port=9222`)
  - YouTube transcript extraction
  - `gh` CLI auth and repo management
  - Real automation examples (GitHub search, click actions, screenshots)
  - Quick command reference
